{
/*
     // Place your snippets for Python here. Each snippet is defined under a snippet name and has a prefix, body and
     // description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
     // $1, $2 for tab stops, ${id} and ${id:label} and ${1:label} for variables. Variables with the same id are connected.
     // Example:
     "Print to console": {
        "prefix": "log",
        "body": [
            "console.log('$1');",
            "$2"
        ],
        "description": "Log output to console"
    }
*/
    "pdb": {
        "prefix": "pdb",
        "body": [
            "from pdb import set_trace; set_trace()"
        ],
        "description": "Standard debugger."
    },
    "pudb": {
        "prefix": "pudb",
        "body": [
            "from pudb import set_trace; set_trace()"
        ],
        "description": "PuDB debugger (https://pypi.python.org/pypi/pudb)."
    },
    "ipdb": {
        "prefix": "ipdb",
        "body": [
            "from ipdb import set_trace; set_trace()"
        ],
        "description": "IPython debugger (https://pypi.python.org/pypi/ipdb)."
    },
    "pp": {
        "prefix": "pprint",
        "body": [
            "from pprint import pprint as pp",
            "pp($1)"
        ],
        "description": "Import pprint as pp"
    },
    "q": {
        "prefix": "q",
        "body": [
            "import q; q($1)"
        ],
        "description": "Check output in tail -f /tmp/q or tail -f /$TMPDIR/q. https://pypi.python.org/pypi/q"
    },
    "akw": {
        "prefix": "akw",
        "body": [
            "*args, **kwargs"
        ],
        "description": "*args, **kwargs"
    },
    "test": {
        "prefix": "test",
        "body": [
            "def test_$1(self):",
            "    $2"
        ],
        "description": "def test_foo(self): ..."
    },
    "property": {
        "prefix": "property",
        "body": [
            "@property",
            "def $1(self):",
            "    $2"
        ],
        "description": "@property"
    }
}
